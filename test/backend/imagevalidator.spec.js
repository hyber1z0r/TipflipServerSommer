/**
 * Created by jakobgaardandersen on 12/08/15.
 */
var imageValidator = require('../../server/modules/imageValidator');
var should = require('should');

describe('ImageValidator', function () {
    it('should should pass', function (done) {
        'true'.should.equal('true');
        done();
    });
});